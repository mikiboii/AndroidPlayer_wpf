<UserControl x:Class="Androidplayer_wpf.Src.Pages.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Androidplayer_wpf.Src.Pages"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="800"
             Background="#2D2D30"
             
             >

    
    <Border Background="#2D2D30" Height="30">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- App Icon and Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                <!-- <TextBlock Text="🚀" FontSize="14" VerticalAlignment="Center" Margin="0,0,5,0"/> -->
                <Image  Source="./../../Icons/android.png" Height="25"   RenderOptions.BitmapScalingMode="HighQuality" Margin="0,0,10,0" />
                
                <TextBlock Name="app_name" MouseDown="DragRegion_OnMouseDown" IsHitTestVisible="False" Text="{Binding Title}" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- Drag Region -->
            <Border Grid.Column="1" Background="Transparent" MouseDown="DragRegion_OnMouseDown"/>
            
            <!-- Window Controls -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <!-- Minimize Button -->
                <Button x:Name="MinimizeBtn" Content="─" Width="46" Height="30" 
                        Background="Transparent" Foreground="White" BorderThickness="0"
                        Click="MinimizeBtn_OnClick" FontSize="12" FontWeight="Bold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#3E3E42"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- Maximize/Restore Button -->
                <Button x:Name="MaximizeBtn" Content="□" Width="46" Height="30" 
                        Background="Transparent" Foreground="White" BorderThickness="0"
                        Click="MaximizeBtn_OnClick" FontSize="10">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#3E3E42"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="CloseBtn" Content="×" Width="46" Height="30"
                        Background="Transparent" Foreground="White" BorderThickness="0"
                        Click="CloseBtn_OnClick" FontSize="14" FontWeight="Bold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E81123"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                     </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>