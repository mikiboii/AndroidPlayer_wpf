

<Window x:Class="Androidplayer_wpf.windows.settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Androidplayer_wpf.windows"
        
        xmlns:settings_view="clr-namespace:Androidplayer_wpf.windows.settings_view"
        
        mc:Ignorable="d"
        Title="Settings"
        Height="600" Width="820"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F6F7F9"
        Icon="../Icons/android.png">

    <WindowChrome.WindowChrome>
        <WindowChrome x:Name="chrome" CornerRadius="8,8,0,0" GlassFrameThickness="0" CaptionHeight="0"/>
    </WindowChrome.WindowChrome>

    <Grid Margin="0" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="228"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Close button - TOP LEFT (moved per your request) -->
        

        <!-- Left Navigation Panel -->
        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="12" Margin="12,12,0,0">
            <StackPanel>
                <TextBlock Text="Settings" FontWeight="Bold" FontSize="18" Margin="6,4,0,18" />
                <ListBox x:Name="SettingsListBox" BorderThickness="0" Background="Transparent"
                         SelectionMode="Single" SelectedIndex="3"
                         FontSize="14" Foreground="#2D2D2D">
                    <ListBoxItem Padding="10">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                            <TextBlock Text="📺" Margin="0,0,10,0"/>
                            <TextBlock Text="Screen Settings" />
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Padding="10">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                            <TextBlock Text="🪟" Margin="0,0,10,0"/>
                            <TextBlock Text="Window settings" />
                        </StackPanel>
                    </ListBoxItem>

                    <!-- <ListBoxItem Padding="10"> -->
                    <!--     <StackPanel Orientation="Horizontal" VerticalAlignment="Center" > -->
                    <!--         <TextBlock Text="⌨️" Margin="0,0,10,0"/> -->
                    <!--         <TextBlock Text="Input Settings" /> -->
                    <!--     </StackPanel> -->
                    <!-- </ListBoxItem> -->

                    <!-- Active item styling -->
                    <ListBoxItem Padding="10" Background="#F2F6FF" FontWeight="SemiBold">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                            <TextBlock Text="🖱️" Margin="0,0,10,0"/>
                            <TextBlock Text="Mouse Setting" />
                        </StackPanel>
                    </ListBoxItem>

                    <!-- <ListBoxItem Padding="10"> -->
                    <!--     <StackPanel Orientation="Horizontal" VerticalAlignment="Center" > -->
                    <!--         <TextBlock Text="⚡" Margin="0,0,10,0"/> -->
                    <!--         <TextBlock Text="Trickle-Power settings" /> -->
                    <!--     </StackPanel> -->
                    <!-- </ListBoxItem> -->

                    <!-- <ListBoxItem Padding="10"> -->
                    <!--     <StackPanel Orientation="Horizontal" VerticalAlignment="Center" > -->
                    <!--         <TextBlock Text="✔️" Margin="0,0,10,0"/> -->
                    <!--         <TextBlock Text="Check settings" /> -->
                    <!--     </StackPanel> -->
                    <!-- </ListBoxItem> -->

                    <ListBoxItem Padding="10">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                            <TextBlock Text="⌨️" Margin="0,0,10,0"/>
                            <TextBlock Text="Shortcut key" />
                        </StackPanel>
                    </ListBoxItem>
                    <!-- -->
                    <!-- <ListBoxItem Padding="10"> -->
                    <!--     <StackPanel Orientation="Horizontal" VerticalAlignment="Center" > -->
                    <!--         <TextBlock Text="{}" Margin="0,0,10,0"/> -->
                    <!--         <TextBlock Text="Js setting" /> -->
                    <!--     </StackPanel> -->
                    <!-- </ListBoxItem> -->
                    <!-- -->
                    <!-- <ListBoxItem Padding="10"> -->
                    <!--     <StackPanel Orientation="Horizontal" VerticalAlignment="Center" > -->
                    <!--         <TextBlock Text="🛠️" Margin="0,0,10,0"/> -->
                    <!--         <TextBlock Text="Custom Toolbar" /> -->
                    <!--     </StackPanel> -->
                    <!-- </ListBoxItem> -->
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Separator -->
        <Border Grid.Column="1" Width="1" Background="#E6E9EE" Margin="0,10"/>

       <!-- Main content area composed of content + footer -->
<Grid Grid.Column="2" Margin="0,0,0,0">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>   <!-- for top bar with close button -->
        <RowDefinition Height="*"/>      <!-- for scrollable content -->
        <RowDefinition Height="72"/>     <!-- for footer buttons -->
    </Grid.RowDefinitions>

    <!-- Top bar with image-based close button -->
    <Grid Grid.Row="0" Margin="0,0,0,8" Height="50" HorizontalAlignment="Stretch"  >
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
       
        <Border Grid.Column="0" Background="Transparent" MouseLeftButtonDown="TopBar_MouseLeftButtonDown" >
            
            
            <Image  Source="../Icons/close.png"
                    Margin="0,12,12,0"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Width="22" Height="22"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Top"
                   Cursor="Hand"
                   MouseLeftButtonUp="CloseButton_Click"
                   IsHitTestVisible="True"/>
            
        </Border>
        
        
    </Grid>


    
    
    <!-- <local:settings  ></local:settings> -->
    
    <!-- <settings_view:MouseSettings Grid.Row="1" ></settings_view:MouseSettings> -->
    
    
    <!-- Main content area -->
    <ContentControl x:Name="SettingsContent" Grid.Row="1" Margin="0,0,0,12"/>

    
    <!-- ~1~ Scrollable content card @1@ -->
    <!-- <Border Grid.Row="1" -->
    <!--         Background="White" -->
    <!--         CornerRadius="8" -->
    <!--         Padding="24" -->
    <!--         Margin="0,0,0,12" -->
    <!--         BorderBrush="#E8EBF0" -->
    <!--         BorderThickness="1"> -->
    <!--     <ScrollViewer VerticalScrollBarVisibility="Auto"> -->
    <!--         <StackPanel> -->
    <!-- -->
    <!--             ~1~ Key mouse unlock @1@ -->
    <!--             <Border CornerRadius="6" Background="#FFFFFF" Padding="14" Margin="0,0,0,16" BorderBrush="#EEF1F4" BorderThickness="1"> -->
    <!--                 <StackPanel> -->
    <!--                     ~1~ Foreground="#26292D" @1@ -->
    <!--                     <TextBlock Foreground="{DynamicResource  PrimaryBrush}"  Text="Key mouse unlock" FontWeight="Bold" FontSize="15" Margin="0,0,0,4"/> -->
    <!--                     <TextBlock  Text="Global Settings" FontSize="12" Foreground="#6B6F77" Margin="0,0,0,12"/> -->
    <!--                     <CheckBox  Content="Whether to turn on screen center unlock" IsChecked="True" Margin="0,0,0,8"/> -->
    <!--                     <TextBlock Text="* After checking, the position of the mouse after unlocking will always be displayed centered relative to the screen" -->
    <!--                                Foreground="#E36C60" FontSize="12" TextWrapping="Wrap"/> -->
    <!--                 </StackPanel> -->
    <!--             </Border> -->
    <!-- -->
    <!--             ~1~ Windows System Settings @1@ -->
    <!--             <Border CornerRadius="6" Background="#FFFFFF" Padding="14" Margin="0,0,0,16" BorderBrush="#EEF1F4" BorderThickness="1"> -->
    <!--                 <StackPanel> -->
    <!--                     <TextBlock Text="Windows System Settings" FontWeight="Bold" FontSize="15" Margin="0,0,0,4"/> -->
    <!--                     <TextBlock Text="Global Settings" FontSize="12" Foreground="#6B6F77" Margin="0,0,0,12"/> -->
    <!--                     <CheckBox Content="Mouse acceleration (improve pointer accuracy)" IsChecked="False" Margin="0,0,0,8"/> -->
    <!--                     <TextBlock Text="* After checking, it will take effect globally in Windows" -->
    <!--                                Foreground="#E36C60" FontSize="12" TextWrapping="Wrap"/> -->
    <!--                 </StackPanel> -->
    <!--             </Border> -->
    <!-- -->
    <!--             ~1~ Mouse speed @1@ -->
    <!--             <Border CornerRadius="6" Background="#FFFFFF" Padding="14" Margin="0,0,0,8" BorderBrush="#EEF1F4" BorderThickness="1"> -->
    <!--                 <StackPanel> -->
    <!--                     <TextBlock Text="Mouse speed" FontWeight="Bold" FontSize="15" Margin="0,0,0,4"/> -->
    <!--                     <TextBlock Text="Global Settings" FontSize="12" Foreground="#6B6F77" Margin="0,0,0,12"/> -->
    <!--                     <RadioButton Content="Default" IsChecked="True" GroupName="MouseSpeed"/> -->
    <!--                 </StackPanel> -->
    <!--             </Border> -->
    <!-- -->
    <!--         </StackPanel> -->
    <!--     </ScrollViewer> -->
    <!-- </Border> -->

    <!-- Footer with Cancel + Save aligned to the right -->
    <Border Grid.Row="2" Background="Transparent">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,6,0">
                <Button Content="Cancel" Width="100" Height="34" Margin="0,0,10,0"
                        Style="{StaticResource RoundedButtonStyle}" Click="CloseButton_Click"/>
                <Button Content="Save Changes" Width="130" Height="34"
                        Click="save_btn_click"
                        Style="{StaticResource RoundedPrimaryButtonStyle}"/>
            </StackPanel>
            
             
        </Grid>
    </Border>
</Grid>

    </Grid>
</Window>
